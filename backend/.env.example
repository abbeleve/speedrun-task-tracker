# Optional: override the SQLite database location.
# Defaults to <repo>/backend/data/tracker.db when unset.
# DATABASE_PATH=/var/lib/speedrun/tracker.db

# Bind host/port (used by uvicorn, e.g. started via run.py)
# HOST=0.0.0.0
# PORT=8000
